# ======================== Elasticsearch Configuration =========================
# Estas lineas de configuracion son las que hay que añadir
cluster.name: Blind-Eternities
node.name: Emrakul
network.host: 0.0.0.0
transport.host: 0.0.0.0
node.roles: [ master, ingest, data]
http.port: 9200
transport.port: 9300

# ========================= Fin de la Configuracion ============================
# A partir de aqui, el resto del archivo es autogenerado una vez que inicies por primera vez el nodo



#----------------------- BEGIN SECURITY AUTO CONFIGURATION -----------------------
#
# The following settings, TLS certificates, and keys have been automatically
# generated to configure Elasticsearch security features on 26-11-2025 10:25:42
#
# --------------------------------------------------------------------------------

# Enable security features
xpack.security.enabled: true

xpack.security.enrollment.enabled: true

# Enable encryption for HTTP API client connections, such as Kibana, Logstash, and Agents
xpack.security.http.ssl:
  enabled: true
  keystore.path: certs/http.p12

# Enable encryption and mutual authentication between cluster nodes
xpack.security.transport.ssl:
  enabled: true
  verification_mode: certificate
  keystore.path: certs/transport.p12
  truststore.path: certs/transport.p12
# Create a new cluster with the current node only
# Additional nodes can still join the cluster later
cluster.initial_master_nodes: ["Emrakul"]

#----------------------- END SECURITY AUTO CONFIGURATION -------------------------