###################### Metricbeat Configuration Example #######################
 
# This file is an example configuration file highlighting only the most common
# options. The metricbeat.reference.yml file from the same directory contains all the
# supported options with more comments. You can use it as a reference.
#
# You can find the full configuration reference here:
# https://www.elastic.co/guide/en/beats/metricbeat/index.html
 
# =========================== Modules configuration ============================
 
metricbeat.config.modules:
  # Glob pattern for configuration loading
  path: ${path.config}/modules.d/*.yml
 
  # Set to true to enable config reloading
  reload.enabled: false
 
  # Period on which files under path should be checked for changes
  #reload.period: 10s
 
# ======================= Elasticsearch template setting =======================
 
setup.template.settings:
  index.number_of_shards: 1
  index.codec: best_compression
  #_source.enabled: false
 
# Starting with Beats version 6.0.0, the dashboards are loaded via the Kibana API.
# This requires a Kibana endpoint configuration.
setup.kibana:
  host: "http://192.199.1.55:5601" # cambiar por la IP y puerto reales de Kibana (si es en local, puedes usar localhost)
  username: "elastic"              # ejemplo de usuario, cambiar por el real
  password: "${ES_PASSWORD}"       # usar variable de entorno o metricbeat keystore
 
# ================================== Outputs ===================================
 
# Configure what output to use when sending the data collected by the beat.
 
# ---------------------------- Elasticsearch Output ----------------------------
output.elasticsearch:
  # Array of hosts to connect to.
  # hay 3 hosts para enviar el estado de elastic al resto del cluster
  hosts: ["192.199.1.55:9200", "192.199.1.56:9200", "192.199.1.57:9200"] # esta ip es representativa, cambiar por la IP real de Elasticsearch (o localhost)
 
  # Performance preset - one of "balanced", "throughput", "scale",
  # "latency", or "custom".
  preset: balanced
 
  # Protocol - either `http` (default) or `https`.
  protocol: "https"
 
  # Authentication credentials - either API key or username/password.
  #api_key: "id:api_key"
  username: "elastic"
  password: "${ES_PASSWORD}"       # usar variable de entorno o metricbeat keystore
  ssl:
     certificate_authorities: ["/home/g1/elasticsearch-9.2.1/config/certs/http_ca.crt"] # ruta del certificado CA de Elasticsearch (apuntar al certificado)
# ------------------------------ Logstash Output -------------------------------
